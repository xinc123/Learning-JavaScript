事件传播分捕获、目标和冒泡三个阶段。
*同一事件会在多个节点上触发。

示例代码中click事件触发了四次：
    1.捕获阶段(capture)：事件从<ul>向<li>传播时，触发<ul>的click事件；
    2.目标阶段(target)：事件从<ul>到达<li>时，触发<li>的click事件；
    3.冒泡阶段(bubble)：事件从<li>传回<ul>时，再次触发<ul>的click事件。

浏览器假定点击位置嵌套最深的节点为事件目标节点，所以<li>节点的捕获和冒泡阶段都为target阶段。